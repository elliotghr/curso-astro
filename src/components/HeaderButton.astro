---
interface Props {
  href: string;
}

const { href = "/" } = Astro.props;
---

<a
  class="flex items-center justify-center w-full gap-1 px-5 py-4 text-xl duration-300 md:w-auto md:py-2 md:text-base hover:scale-90"
  href={href}
>
  <slot name="before" />
  <slot />
  <slot name="after" />
</a>
